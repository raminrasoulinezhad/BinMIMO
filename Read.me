Main files are:

	GAMP_cpuFuncs.cu
	GAMP_gpuFuncs.cu
	GAMP_main.cu


## Ramin:
	cd model
	make all
	make run
	

## Bahari:
To run this code you need to run 

	export PATH=/usr/local/cuda/bin:/usr/local/cuda/NsightCompute-1.0${PATH:+:${PATH}}
	
	nvcc -o GAMP_main.out -c GAMP_main.cu
	chmod +x ./GAMP_main.out 	
	./GAMP_main.out 

Result:

	-bash: ./GAMP_main.out: cannot execute binary file: Exec format error

	
